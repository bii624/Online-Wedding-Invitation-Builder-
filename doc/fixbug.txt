 Kịch bản TỆ: Dùng useState cho cả Tọa độ chuột lẫn Tọa độ phần tửChuột di chuyển 1 pixel.Cập nhật startX, startY qua useState $\rightarrow$ React Re-render lần 1 để ghi nhớ tọa độ chuột mới.Tính toán xong, cập nhật vị trí phần tử qua Zustand/State $\rightarrow$ React Re-render lần 2 để dịch chuyển phần tử trên màn hình.👉 Kết quả: Component bị render 2 lần cho mỗi một pixel di chuyển. Giao diện giật lag điên cuồng vì React phải tính toán lại toàn bộ cấu trúc cây thư mục (DOM) liên tục.Kịch bản TỐT (Code hiện tại của bạn): Kết hợp useRef + ZustandChuột di chuyển 1 pixel.Cập nhật startX, startY vào useRef $\rightarrow$ Không re-render (Lặng lẽ ghi đè bộ nhớ, mất 0 mili-giây).Lấy giá trị đó tính toán nhanh, rồi chỉ cập nhật vị trí phần tử qua Zustand $\rightarrow$ React Re-render đúng 1 lần duy nhất để dịch chuyển phần tử trên màn hình.👉 Kết quả: Giảm được 50% số lần re-render thừa thãi. Hệ thống chỉ tập trung tài nguyên vào việc duy nhất: Vẽ lại vị trí mới của phần tử.